# Reproducible Validation of Air Pollution Retrieval

# 小组成员

丁俐杉2025303110114
于辛铠2025303110116
成一潇2025303110122
霍晶晶2025303110132

## 项目背景

PM2.5 是评价空气污染程度的重要指标之一。传统的地面监测站点可以提供较准确的观测数据，但监测站点在空间分布上通常有限，难以全面反映区域尺度上的污染变化情况。

遥感反演方法可以利用卫星观测数据估算空气污染物浓度，从而在更大空间范围内补充地面观测数据。但是，遥感反演结果是否可靠，需要通过地面实测数据进行验证。

因此，本项目选取 PM2.5 反演值与地面观测值之间的关系作为分析对象，通过统计指标和可视化图像判断反演结果的精度表现。

## 参考文献

Song, Z., Bai, Y., Wang, D., Li, T., & He, X. (2021).  
*Satellite Retrieval of Air Pollution Changes in Central and Eastern China during COVID-19 Lockdown Based on a Machine Learning Model.*  
Remote Sensing, 13(13), 2525.

## 数据说明

本项目使用的数据文件为：

```text
pm25.csv
```

数据中主要包含与 PM2.5 相关的观测值、预测值或反演值，以及可能用于辅助分析的空间位置信息。项目通过读取该数据文件，提取需要分析的字段，并完成后续的精度验证与图像绘制。

本项目中的数据主要用于课程实验和方法复现，重点关注如何从已有数据出发，完成一个清晰、可运行、可解释的数据分析流程。

## 项目结构

建议仓库目录结构如下：

```text
.
├── README.md
├── requirements.txt
├── pm25.csv
├── 03散点密度图（遥感反演数据精度验证）.ipynb
├── output.png
└── result.jpg
```

各文件说明如下：

| 文件名 | 说明 |
|---|---|
| `README.md` | 项目说明文档，用于介绍项目背景、方法、运行方式和结果解释 |
| `requirements.txt` | Python 环境依赖文件 |
| `pm25.csv` | 本项目使用的 PM2.5 数据文件 |
| `03散点密度图（遥感反演数据精度验证）.ipynb` | 主要代码文件，包含数据读取、预处理、指标计算和绘图过程 |
| `output.png` | 数据初步可视化结果 |
| `result.jpg` | 最终散点密度图结果 |

## 环境依赖

本项目使用 Python 完成数据分析和图像绘制，主要依赖以下第三方库：

```text
pandas
numpy
matplotlib
seaborn
scikit-learn
scipy
jupyter
```

## 方法说明

### 1. 散点图分析

普通散点图可以直观展示两个变量之间的关系。在本项目中，横轴表示地面观测 PM2.5，纵轴表示反演 PM2.5。

如果散点大致分布在 1:1 参考线附近，说明反演值与实测值比较接近；如果散点整体偏离参考线，则说明反演结果可能存在系统性偏高或偏低的问题。

### 2. 散点密度图分析

当样本数量较多时，普通散点图容易出现点重叠的问题，难以观察样本主要集中在哪些区域。因此，本项目进一步绘制散点密度图。

散点密度图通过颜色表示样本点的集中程度，颜色越明显，表示该区域的数据点越密集。相比普通散点图，散点密度图更适合展示大样本数据的整体分布特征。

### 3. 拟合线与参考线

在结果图中：

- 黑色虚线表示 1:1 参考线；
- 红色实线表示拟合线；
- 散点表示不同样本点；
- 颜色表示散点密度。

如果拟合线与 1:1 参考线越接近，说明反演值与地面观测值之间的一致性越好。

## 评价指标

本项目使用以下指标对反演结果进行评价：

| 指标 | 含义 | 解释 |
|---|---|---|
| N | 样本数量 | 参与计算的有效样本数 |
| R | 相关系数 | 反映预测值与实测值之间的相关程度 |
| BIAS | 平均偏差 | 反映预测值整体偏高或偏低 |
| RMSE | 均方根误差 | 反映预测误差的整体大小 |

一般来说：

- `R` 越接近 1，说明预测值与实测值之间的线性相关性越强；
- `BIAS` 越接近 0，说明整体系统偏差越小；
- `RMSE` 越小，说明预测误差越小；
- 样本数量 `N` 越大，结果通常越具有代表性。

这些指标能够从不同角度评价反演结果的可靠性。


## 结果分析

通过实验结果可以看出，本项目中的 PM2.5 反演结果整体表现较好。多数数据点靠近 1:1 参考线，说明反演结果与地面监测结果之间具有较好的一致性。

从评价指标来看，较高的相关系数说明模型反演结果能够较好地反映 PM2.5 浓度变化趋势；较低的 RMSE 表明整体误差处于可接受范围；BIAS 可以进一步判断反演值是否存在整体高估或低估。

不过，从图中也可以观察到，部分高浓度样本点存在一定偏离。这说明在污染浓度较高的情况下，反演结果可能会出现一定误差。这种现象在实际遥感反演任务中较为常见，可能与样本数量、空间分布、气象条件、模型误差等因素有关。

总体而言，本项目完成了对 PM2.5 反演数据的基本精度验证，并通过可视化方式较清晰地展示了反演值与地面观测值之间的关系。

## 可重复性说明

为了保证项目具有可重复性，本仓库做了以下整理：

1. 保留原始数据文件 `pm25.csv`；
2. 提供完整的 Notebook 文件；
3. 在 README 中说明项目背景、数据、方法和运行方式；
4. 提供 `requirements.txt`，方便安装依赖；
5. 保存主要输出图像，便于与重新运行结果进行对比；
6. 代码按照数据读取、数据处理、指标计算和结果可视化的顺序组织。

在依赖库正确安装、数据文件路径不变的情况下，运行 Notebook 可以复现项目中的主要图像和统计结果。


## 项目特点

本项目虽然规模较小，但尽量按照完整的数据分析项目进行组织，主要体现以下特点：

- 数据、代码和结果放在同一仓库中，便于检查和复现；
- README 对项目背景、方法、运行步骤和结果进行了说明；
- 使用 Notebook 展示完整分析过程，便于阅读和修改；
- 输出图像能够直观展示实验结果；
- 项目结构清晰，符合课程对可重复性实验的基本要求。

## 总结

本项目围绕 PM2.5 遥感反演结果的精度验证展开，完成了从数据读取、指标计算到结果可视化的完整流程。

通过散点密度图可以直观观察反演值与地面观测值之间的关系；通过 R、BIAS 和 RMSE 等指标可以从定量角度评价反演结果的准确性。实验结果表明，反演数据与地面观测数据之间具有较好的相关性，说明该方法能够较好地反映 PM2.5 的空间分布和浓度变化特征。

本项目的主要意义在于将一个数据分析任务整理为可运行、可检查、可复现的 GitHub 项目，符合“数据驱动与可重复性实验”课程对项目管理、结果解释和实验复现的要求。
